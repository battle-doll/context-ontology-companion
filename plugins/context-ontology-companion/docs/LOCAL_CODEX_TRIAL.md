# Local Codex project trial — 2026-09-05

macOS, Python 3.12.14, Codex 0.153.3. Manual project-local stdio
connections passed a real app-server smoke: Context seven read tools and
Contracts three tools discovered and invoked, 12 calls across 10 unique tools.
The host session was ephemeral with zero model turns. A repo-scoped trial Skill
was discovered. This does not establish packaged-plugin installation or hosted
ChatGPT E2E.

The current workspace lifecycle harness passed 160 assertions and validated 46
tool responses against outputSchema. Three selected README source observations
carry explicit SIMULATION ONLY labels, line/file SHA provenance, and
simulated_not_human_evidence approval. Pending exclusion, idempotency, process
restart, access denial, atomic budget/conflict groups, erase/history absence,
export and independent Contracts validation passed. Real human approval remains
unverified by this trial.

Actual host testing exposed a Context tools/list interoperability defect:
optional metadata and empty/null first-page cursors were rejected. The transport
now accepts them, while continuation tokens, wrong types, unknown fields and
pre-initialization calls remain rejected. Three regression tests were added.
Product unit suites passed 41 Context and 41 Contracts cases.

Windows/Linux execution, Windows ACL, model/task-quality benchmarks, marketplace
installation, public repositories, hosted ChatGPT, real user approval and
Directory submission/publication remain separate gates.

Reproduction in the local parent workspace: scripts/project_trial.py and
scripts/verify_codex_trial_host.py. Machine-specific settings and synthetic
runtime storage are outside both product distributions. Raw final evidence is
under the parent verification/project-trial-final and
verification/codex-trial-host-final.json.
