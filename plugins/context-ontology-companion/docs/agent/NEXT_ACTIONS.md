# Next actions — 0.1.0

1. Local MCP recovery and local checks passed; run current-version platform CI.
2. Publish the reviewed GitHub0.1.0 release and update local installation/configured MCP paths.
3. Upload new portal draft; the owner handles policy attestation, submission and Publish.
4. Keep remote hosting, multi-tenant authentication and stable contract migration separate from supported local OS-account use.
